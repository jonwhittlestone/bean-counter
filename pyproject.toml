[project]
name = "bean-counter"
version = "0.1.0"
description = "Add a short description here"
authors = [
    { name = "Jon Whittlestone", email = "dev@howapped.com" }
]
dependencies = ["jupyterlab~=4.0.0"]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
